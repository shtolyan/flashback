# HexLive → Flashback: переключение 10 сентября 2026

Рабочий UI: https://flashback.62-146-235-120.sslip.io.
API: тот же origin + `/api/bugs/v1`.

- Хост: Сингапур, `62.146.235.120`, SSH `hexlive-server`.
- Активный релиз: `/opt/flashback/releases/8b22066d76f623f751421ca1d88df596774386fd`; ссылка `/opt/flashback/current`.
- Образ: `flashback:8b22066d76f623f751421ca1d88df596774386fd`.
- Compose project `flashback`, приложение `flashback-app-1`, PostgreSQL `flashback-db-1`, постоянный том `flashback_pgdata`.
- Caddy слушает HTTPS; приложение доступно только через `127.0.0.1:4310`, порт PostgreSQL не опубликован.

## Синхронизация

Перед переключением исходные 344 отчёта и локальная копия совпадали. В PostgreSQL перенесены 344 отчёта, 1115 комментариев, 398 ссылок на коммиты и 29 патчей. Импорт сверил каждое поле, ID, ревизию, дату и патч.

На старом HTTPS endpoint временно блокировались только записи баг-трекера и старый admin UI. Сделана заключительная SQLite-копия и HTTP-выгрузка; повторная проверка подтвердила полное совпадение с перенесёнными данными и локальным Flashback. Игра и сохранения не переносились и не перезапускались.

SHA-256 содержимого импортированного снимка (канонизированные reports + patches): `279412c2a648843bfceab9bc98e8f49228cbd1d1ff278ba639780fdfe177dd89`.

## Старые клиенты

Caddy в Нью-Йорке продолжает обслуживать игру и Asset API. Только `/api/bugs/v1` проксируется по HTTPS в Сингапур. Старые игровые клиенты, агенты и токены совместимы; пересборка игры для переключения не требуется.

`/admin/bugs` перенаправляется на новый UI, `/admin/bugs/<id>` — на соответствующую карточку. POST из уже открытой старой формы получает явный 409 вместо записи в старую SQLite. Конфигурации до/после сохранены; `deploy/Caddyfile.hexlive-nyc` — действующий маршрут.

В рабочей папке HexLive обновлены canonical `Server/Caddyfile`, адрес по умолчанию CLI и обоих build scripts, руководство агента и §114. Там уже были посторонние незакоммиченные изменения и шесть локальных коммитов; они не включены в Flashback и не отправлены в GitHub. Отдельный патч `deploy/hexlive-integration.patch` содержит только изменения интеграции для следующего обычного коммита HexLive.

## Проверка

- Сверены все отчёты и все патчи через API развёрнутого экземпляра.
- В браузере проверены список, карточка, сворачивание боковой панели и публичный HTTPS.
- Через старый API создан и изменён временный отчёт #376. Новый UI немедленно показал его через WebSocket; прямой новый API вернул те же поля.
- Удаление через новый API подтверждено ответом 404 старого API. Тестовая запись не осталась в данных.
- Проверены перенаправления старой админки и отказ старой POST-формы с 409.
- HexLive CLI с новым URL прочитал все 344 отчёта. Оба контейнера healthy, игровые службы и Caddy active.

## Копии и восстановление

На Нью-Йорке: `/var/backups/flashback-cutover-20260910/` содержит исходный Caddyfile, SQLite до и после заморозки, итоговую JSON-выгрузку.

На Сингапуре: `/opt/flashback/backups/` содержит исходный Caddyfile, итоговый JSON, исходную SQLite и `flashback-cutover-20260910.dump`. SHA-256 PostgreSQL dump: `9131833e46133943d8cf6dcc2506c6193a9f618aa587952618642aba06c9fbe3`.

Локальные копии лежат в игнорируемых Git каталогах `backups/` и `data/`. Локальный PostgreSQL оставлен как независимая копия для разработки, не как второе production-хранилище.

Для отката кода сохраняйте текущий PostgreSQL и меняйте только образ приложения. После начала записей в новый трекер нельзя просто вернуть старый Caddyfile/SQLite: сначала нужна обратная синхронизация новых данных. При аварии лучше временно закрыть записи, чем создать две расходящиеся базы.
